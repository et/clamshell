Dependencies.configure do
  git "/here/there/.git", :ref => "12345"

  environment("bash") do
    env_var "PATH", "/usr/bin"
    env_var "PATH", :append  => "/usr/bin"
    env_var "PATH", :prepend => "/usr/bin"
  end
end
